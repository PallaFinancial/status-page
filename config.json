[
  {
    "label": "Palla Auth Email",
    "key": "pallaAuthEmail",
    "url": "https://api.platform.palla.app/health/v1/auth",
    "method": "POST",
    "env": "production",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "production", "api"],
      "description": "Begins the checkout flow authentication. Returns a temporary token used to auth and verify a phone via SMS."
    }
  },
  {
    "label": "Palla Checkout Web",
    "key": "pallaCheckoutWeb",
    "url": "https://checkout.palla.app",
    "method": "GET",
    "env": "production",
    "type": "web",
    "meta": {
      "tags": ["web", "production", "partner", "palla.app"],
      "description": "Checkout page for palla.app partner"
    }
  },
  {
    "label": "Palla Auth Phone",
    "key": "pallaAuthPhone",
    "url": "https://api.platform.palla.app/health/v1/auth",
    "method": "POST",
    "env": "production",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "production", "api"],
      "description": "Add a phone for SMS authentication."
    }
  },
  {
    "label": "Palla Auth Verify",
    "key": "pallaAuthVerify",
    "url": "https://api.platform.palla.app/health/v1/auth/verify",
    "method": "POST",
    "env": "production",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "production", "api"],
      "description": "Verify the OOB code sent to the user. On success, receive an access token."
    }
  },
  {
    "label": "Palla Token Upgrade",
    "key": "pallaTokenUpgrade",
    "url": "https://api.platform.palla.app/health/v1/auth",
    "method": "POST",
    "env": "production",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "production", "api"],
      "description": "Updates an existing token's scope."
    }
  },
  {
    "label": "Palla Auth Email",
    "key": "pallaAuthEmail",
    "url": "https://api.platform.palla.app/health/v1/auth",
    "method": "POST",
    "env": "sandbox",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "sandbox", "api"],
      "description": "Begins the checkout flow authentication. Returns a temporary token used to auth and verify a phone via SMS."
    }
  },
  {
    "label": "Palla Auth Phone",
    "key": "pallaAuthPhone",
    "url": "https://api.platform.palla.app/health/v1/auth",
    "method": "POST",
    "env": "sandbox",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "sandbox", "api"],
      "description": "Add a phone for SMS authentication."
    }
  },
  {
    "label": "Palla Auth Verify",
    "key": "pallaAuthVerify",
    "url": "https://api.platform.palla.app/health/v1/auth/verify",
    "method": "POST",
    "env": "sandbox",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "sandbox", "api"],
      "description": "Verify the OOB code sent to the user. On success, receive an access token."
    }
  },
  {
    "label": "Palla Token Upgrade",
    "key": "pallaTokenUpgrade",
    "url": "https://api.platform.palla.app/health/v1/auth",
    "method": "POST",
    "env": "sandbox",
    "type": "api",
    "meta": {
      "tags": ["user", "auth", "POST", "sandbox", "api"],
      "description": "Updates an existing token's scope."
    }
  }
]
